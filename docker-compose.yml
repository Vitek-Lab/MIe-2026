services:
  web:
    image: node:20-alpine
    working_dir: /site
    command: sh -c "npm install && npm run dev"
    ports:
      - "3000:3000"
    volumes:
      - ./:/site
      - node_modules:/site/node_modules

volumes:
  node_modules:
